# Referência para dev local com Postgres no Docker (não usar na VPS)
# Use com: docker compose -f docker-compose.yml -f docker-compose.dev.yml --profile local-db up -d
#
# Na VPS, use apenas o .env com suas credenciais reais e:
# docker compose up --build -d
